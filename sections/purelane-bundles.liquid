{{ 'purelane-base.css' | asset_url | stylesheet_tag }}
{{ 'purelane-bundles.css' | asset_url | stylesheet_tag }}

<section class="pl-sec" id="bundles">
  <div class="pl-wrap">
    <div class="pl-glass pl-sec-pad pl-rv">
    
    <div class="pl-panel-head">
      {%- if section.settings.kicker != blank -%}
        <span class="pl-kicker">{{ section.settings.kicker }}</span>
      {%- endif -%}
      {%- if section.settings.heading != blank -%}
        <h2 class="pl-d2" style="margin-top:12px">{{ section.settings.heading }}</h2>
      {%- endif -%}
      {%- if section.settings.subheading != blank -%}
        <p class="pl-lede">{{ section.settings.subheading }}</p>
      {%- endif -%}
    </div>

    <div class="pl-tiers">
      {%- for block in section.blocks -%}
        {%- assign delay_class = '' -%}
        {%- if forloop.index == 2 -%}{%- assign delay_class = 'pl-rv-d2' -%}{%- endif -%}
        {%- if forloop.index == 3 -%}{%- assign delay_class = 'pl-rv-d3' -%}{%- endif -%}
        
        <div class="pl-glass pl-tier{% if block.settings.is_best %} pl-tier--best{% endif %} pl-rv {{ delay_class }}" {{ block.shopify_attributes }}>
          {%- if block.settings.tag != blank -%}
            <span class="pl-tier__tag">{{ block.settings.tag }}</span>
          {%- endif -%}

          <div class="pl-tierpix{% if block.settings.quantity == '5' %} pl-tierpix--five{% endif %}">
            {%- assign qty = block.settings.quantity | plus: 0 -%}
            {%- if qty == 0 -%}{%- assign qty = 3 -%}{%- endif -%}
            {%- for i in (1..qty) -%}
              {% render 'purelane-icons', name: 'leaf' %}
            {%- endfor -%}
          </div>

          <div class="pl-tier__qty">
            {{ block.settings.quantity }}
            <small>Products</small>
          </div>

          <div class="pl-tier__price">
            ₹{{ block.settings.price }}
            {%- if block.settings.compare_price != blank -%}
              <s>₹{{ block.settings.compare_price }}</s>
            {%- endif -%}
          </div>
          
          {%- if block.settings.per_product != blank -%}
            <div class="pl-tier__per">₹{{ block.settings.per_product }} / product</div>
          {%- endif -%}

          <ul>
            {%- if block.settings.feature_1 != blank -%}
              <li>{% render 'purelane-icons', name: 'check' %} {{ block.settings.feature_1 }}</li>
            {%- endif -%}
            {%- if block.settings.feature_2 != blank -%}
              <li>{% render 'purelane-icons', name: 'check' %} {{ block.settings.feature_2 }}</li>
            {%- endif -%}
            {%- if block.settings.feature_3 != blank -%}
              <li>{% render 'purelane-icons', name: 'check' %} {{ block.settings.feature_3 }}</li>
            {%- endif -%}
          </ul>

          <a href="#" class="pl-btn pl-btn--{{ block.settings.button_style }}">Build Bundle</a>
        </div>
      {%- endfor -%}
    </div>
  </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('in');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.1 });

    document.querySelectorAll('#shopify-section-{{ section.id }} .pl-rv').forEach(el => {
      observer.observe(el);
    });
  });
</script>

{% schema %}
{
  "name": "Purelane Bundles",
  "settings": [
    {
      "type": "text",
      "id": "kicker",
      "label": "Kicker",
      "default": "Build your bundle"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "One box. Every room."
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "Mix and match across kitchen, laundry, home and skin..."
    }
  ],
  "blocks": [
    {
      "type": "tier",
      "name": "Tier",
      "limit": 3,
      "settings": [
        {
          "type": "text",
          "id": "tag",
          "label": "Tag",
          "default": "Starter"
        },
        {
          "type": "text",
          "id": "quantity",
          "label": "Quantity (Number)",
          "default": "2"
        },
        {
          "type": "text",
          "id": "price",
          "label": "Price",
          "default": "349"
        },
        {
          "type": "text",
          "id": "compare_price",
          "label": "Compare at Price",
          "default": "598"
        },
        {
          "type": "text",
          "id": "per_product",
          "label": "Per Product Price",
          "default": "174"
        },
        {
          "type": "checkbox",
          "id": "is_best",
          "label": "Highlight as Best?",
          "default": false
        },
        {
          "type": "text",
          "id": "feature_1",
          "label": "Feature 1",
          "default": "Pick any two"
        },
        {
          "type": "text",
          "id": "feature_2",
          "label": "Feature 2",
          "default": "Free shipping"
        },
        {
          "type": "text",
          "id": "feature_3",
          "label": "Feature 3"
        },
        {
          "type": "select",
          "id": "button_style",
          "label": "Button Style",
          "options": [
            { "value": "primary", "label": "Primary" },
            { "value": "ghost", "label": "Ghost" }
          ],
          "default": "ghost"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Purelane Bundles",
      "blocks": [
        {
          "type": "tier",
          "settings": {
            "tag": "Starter",
            "quantity": "2",
            "price": "349",
            "compare_price": "598",
            "per_product": "174",
            "is_best": false,
            "feature_1": "Pick any two",
            "feature_2": "Free shipping",
            "feature_3": "",
            "button_style": "ghost"
          }
        },
        {
          "type": "tier",
          "settings": {
            "tag": "Most popular",
            "quantity": "3",
            "price": "499",
            "compare_price": "897",
            "per_product": "166",
            "is_best": true,
            "feature_1": "Pick any three",
            "feature_2": "Covers kitchen and laundry",
            "feature_3": "Free shipping",
            "button_style": "primary"
          }
        },
        {
          "type": "tier",
          "settings": {
            "tag": "Whole home",
            "quantity": "5",
            "price": "799",
            "compare_price": "1495",
            "per_product": "160",
            "is_best": false,
            "feature_1": "Pick any five",
            "feature_2": "Every room in one order",
            "feature_3": "Free shipping",
            "button_style": "ghost"
          }
        }
      ]
    }
  ]
}
{% endschema %}
